
@react.component
let make = () => {
  <div>
    <Button
      color="primary"
      disable=false
      disableRipple=false
      href=""
      variant="contained"
    >{React.string("Hello World")}</Button>
  </div>
}
